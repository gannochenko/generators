{
  "compilerOptions": {
    "module": "esnext",
    "target": "es6",
    "outDir": "./build.esm/",
    "types": []
  },
  "extends": "./tsconfig.json",
  "exclude": [
    "node_modules",
    "build",
    "build:esm",
    "build:cjs",
    <% if (is_ui) { %>"**/**/*.test.tsx",<% } %>
    <% if (is_ui) { %>"**/**/*.story.tsx",<% } %>
    <% if (is_ui) { %>"**/**/*.story.ts",<% } %>
    "**/**/*.test.ts"
  ]
}
